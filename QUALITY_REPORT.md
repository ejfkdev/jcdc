# jcdc 反编译质量报告（2026-09-13/14，HEAD 84459658）

## 评估方法
源码 → 各年代 javac 编译 → jcdc 反编译 → 重编译 → 三层对比：
1. **可重编译**（硬门槛）：重编译零错误
2. **行为语义**（金标准）：运行输出精确比对 + 随机化差分测试
3. **字节码一致**（最严形状指标）：javap 指令序列归一化对比

## 第一层：可重编译率 —— 100%
| 语料 | 规模 | 结果 |
|---|---|---|
| FULL corpus | 21 个 JDK 版本 × 630 源文件 | **616/616 重编译通过**（15 个未编译为基线固有） |
| wide corpus | 40 家族 × jdk11/17/26 | 120/120 |
| census | 3 棵完整 java.base 树 × walk/SESE 双管线 | **6/6 全零错误** |
| rt.jar smoke | jdk8 全量 12609 类 × 双管线 | 12609/12609，0 panic |

## 第二层：行为语义等价 —— 全部通过
- **features battery（本次实测）**：59 个行为特征 × jdk8/9/11/17/21/26 → **56/56 运行输出（rc + stdout + stderr）逐字节一致**
- **随机化差分测试（本次新增）**：对字节码差异最大的 AbstractCollection / AbstractList / AbstractMap 三家族（jdk11/17/26 共 9 组合），用 900 轮随机操作（含 null 元素、对抗性 equals、嵌套集合操作）驱动原始类与「反编译→重编译」类：**9/9 输出逐字节一致**
- corpus 全量历次 sweep：**0 run-mismatch**

## 第三层：字节码一致率 —— ~70%，差异已归因
- methods_equal：FULL 6222/8926（69.7%）；wide 413/588 + 337/473 + 360/502
- 对 120 个不一致方法做了**系统化语义归因**（slot/label 规范化 + 分支极性规范化 + goto-stub 吸收 + 1-WL CFG 着色）：
  - 14/40：**CFG 严格同构**（纯寄存器分配/标号/分支极性差异——确定等价）
  - 26/40：CFG 不同构，但主模式为 **javac 循环合并 vs jcdc 源码忠实还原**（如 AbstractCollection.remove 的双循环：原始字节码合并脚手架，反编译按源码渲染两个独立循环）+ 基本块布局轮转
  - 该类别已由第二层的随机化差分测试**实测背书**：行为完全一致
- 结论：~30% 的字节码差异几乎全部是「重新编译后 javac 的布局/合并选择不同」，而非语义偏差

## 已知语义残差（均编译通过、已文档化）
1. ~~huc getInputStream0 短克隆臂~~ **已治愈**（598230c6 PARKCHAIN sibling-interior routing：auth 区渲染为源码精确三臂形状）；双 removeFromCache 重复亦治愈（2df48ef2 prev_copy dedup）
2. ~~KDF chooseProvider 嵌套 try 展平~~ **已治愈**（7dcfae46，本报告早前版本沿用了过期积压记录）
3. **era-byte 家族**（平台生成类，不在 corpus 内）：源码层不可表达形状已系统化消解（d01f62ac hidden 方法对改名 + 84459658 nestmate private 放宽 + override-throws 交集）——p11-plat invoke 包 423 错 → 58 错（-86%）；剩余为生成代码类型见证深水区

## 总评
- 可重编译：**100%**（全部语料档位）
- 行为等价：**100%**（所有运行级测试通过，含新增随机化差分）
- 字节码逐指令一致：**~70%**，其余 30% 经归因与差分实测确认为形状差异
- 已知语义残差：**2 处**（1 中等 + 1 低，均罕见路径且编译干净）
